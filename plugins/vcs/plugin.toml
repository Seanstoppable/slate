[plugin]
name = "VCS"
description = "Version control status (git/hg)"
tags = ["dev", "git", "vcs"]
version = "0.1.0"
author = "Slate Community"

[permissions]
exec = ["git", "hg"]

[config]
engine = { type = "string", required = false, description = "" }
repo_path = { type = "string", required = false, description = "" }
