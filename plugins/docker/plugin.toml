[metadata]
name = "docker"
description = "Shows Docker container status and resource usage"
tags = ["containers", "dev", "docker"]
version = "0.1.0"
author = "Slate Community"

[permissions]
exec = ["docker"]


