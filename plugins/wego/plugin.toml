[plugin]
name = "wego"
description = "Weather display via wego CLI (ASCII art weather)"
tags = ["utility", "weather"]
version = "0.1.0"
author = "Slate Community"
language = "assemblyscript"
os = ["linux", "macos", "windows"]

[permissions]
exec = ["wego"]
