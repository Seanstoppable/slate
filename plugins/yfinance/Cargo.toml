[package]
name = "slate-yfinance"
version = "0.1.0"
edition = "2021"
description = "Yahoo Finance stock quote widget for Slate"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(target_arch = "wasm32")'.dependencies]
extism-pdk = "1"
