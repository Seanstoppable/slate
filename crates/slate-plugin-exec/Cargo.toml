[package]
name = "slate-plugin-exec"
version = "0.1.0"
edition = "2021"
description = "Shared exec_command host wrapper for Slate plugins"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(target_arch = "wasm32")'.dependencies]
extism-pdk = "1"
